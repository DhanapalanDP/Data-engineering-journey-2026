# Calling the library for the math tools!
import math

# Now we can use special math tools, like finding the square root of a number!
my_number = math.pow(5, 2)

print(my_number)
# The screen prints: 5.0

import numpy as np

x = np.array([1, 2, 3, 4, 5])
# The screen prints: array([1, 2, 3, 4, 5])

print(x)

